# heartdisease
# for flask:
# 1. run: pip install -r requirements.txt
# 2. copy provided .flask.env.example into a new .flaskenv file
# 3. run: flask run
# 4. run: npm start
# note: python-dotenv is included, it may be helpful to use it to create a separate env for your project if you dont already have one.
# run: python -m venv env
# run: env\Scripts\activate
# run: .\env\Scripts\activate
